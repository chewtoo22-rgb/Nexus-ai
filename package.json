{
  "name": "nemotron-nexus",
  "version": "3.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev --remote",
    "deploy": "wrangler deploy",
    "setup": "bash setup.sh",
    "db:init": "wrangler d1 execute nemotron-nexus-db --file=./schema.sql --remote",
    "tail": "wrangler tail"
  },
  "dependencies": {
    "agents": "^0.22.0",
    "@cloudflare/voice": "latest",
    "ai": "^4.3.0",
    "workers-ai-provider": "^2.0.0",
    "@modelcontextprotocol/sdk": "^1.0.0",
    "@cloudflare/sandbox": "^0.12.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@cloudflare/puppeteer": "^1.0.0",
    "@cloudflare/workers-types": "^4.20250525.0",
    "typescript": "^5.6.0",
    "wrangler": "^4.0.0"
  }
}
